// Point this at your deployed Render backend URL (no trailing slash).
// Example: "https://heart-disease-api.onrender.com"
// While testing locally with the backend on your machine, leave it as-is.
const API_BASE_URL = "http://127.0.0.1:8000";
